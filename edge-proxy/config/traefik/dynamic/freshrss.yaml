http:
  routers:
    freshrss:
      rule: "Host(`feeds.de-meyer.nl`)"
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: freshrss
  services:
    freshrss:
      loadBalancer:
        servers:
          - url: "http://192.168.1.45:8080"
